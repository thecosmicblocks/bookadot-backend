export enum Environment {
  local = 'local',
  production = 'production',
  dev = 'dev',
  test = 'test',
}

export enum CHAIN_TYPE {
  EVM = 'EVM',
  SOLANA = 'SOLANA',
}

export enum MOVIE_FORMAT {
  IMAX = 'IMAX',
  THREE_D = '3D',
  TWO_D = '2D',
  LASER = 'LASER',
}
